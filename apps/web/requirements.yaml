packages:
  "framer-motion": "For smooth panel transitions in the horizontal tree view"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility to safely merge Tailwind CSS classes"

images: []

notes:
  - Using API hooks generated by Orval, imported directly from @workspace/api-client-react
  - Implementing an optimistic update wrapper to make the spreadsheet grid feel instantaneous
